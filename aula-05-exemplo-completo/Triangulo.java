class Triangulo {
    // Atributos
    double base;
    double altura;

    // Metodos
    double calculaArea() {
        // Dentro de um metodo, podemos acessar os atributos
        double area;
        area = base * altura / 2;
        return area;
    }
}

